{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v node >/dev/null 2>&1; then node \"${CLAUDE_PLUGIN_ROOT}/lib/session-start.mjs\"; else echo \"lull-n-learn needs Node.js: tell the user in one line to install the LTS from nodejs.org, then restart Claude Code.\"; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
